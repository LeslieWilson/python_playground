import React, {Component} from 'react';


const Result = (props) => (
    <div>
    Name: {props.name}
    City: {props.city}
    State: {props.state}
    Country:{props.country}
    Email: {props.email}
    zip:{props.zip}
    </div>
)




export default Result
